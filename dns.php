<!DOCTYPE html>
<html>

<?php
include('components/header.php');
?>
<body>
<?php
include('components/nav.php');
include("components/sidebar.php");


?>	

<center>
		    
		
			<h1>Premium Status</h1>
				    

<center>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-1-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-1-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>
<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem2.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-2-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-2- ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem3.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-3-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-3-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem4.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-4-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-4-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem5.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-5-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-5-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem6.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-6-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-6-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem7.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-7-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-7-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem8.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-8-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-8-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem9.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-9-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-9-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem10.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PREMIUM-10-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PREMIUM-10-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>



				    
		<h1>Vip Status</h1>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-1-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-1-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip2.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-2-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-2-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip3.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-3-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-3-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip4.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-4-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-4-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip5.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-5-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-5-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip6.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-6-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-6-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip7.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-7-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-7-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip8.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-8-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-8-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>



<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip9.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-9-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-9-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.vip10.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-10-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-10-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem11.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-11-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-11-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>



<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem12.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-12-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-12-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem13.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-13-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-13-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem14.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-14-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-14-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.prem15.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>VIP-15-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>VIP-15-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>



		<h1>Private Status</h1>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.ph1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-PH-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-PH-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>



<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.ph1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-PH-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-PH-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>



<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.ph1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-PH-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-PH-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.ph1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-PH-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-PH-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>




<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.priv1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-1-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-1-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.priv2.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-2-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-2-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.priv3.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-3-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-3-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.priv4.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-4-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-4-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.priv5.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "1194"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>PRIVATE-5-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>PRIVATE-5-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


	<h1>Desktop Status</h1>
	
	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.pc1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>DESKTOP-1-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>DESKTOP-1-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.pc2.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>DESKTOP-2-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>DESKTOP-2-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.pc3.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>DESKTOP-3-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>DESKTOP-3-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.pc4.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>DESKTOP-4-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>DESKTOP-4-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.pc5.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "443"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>DESKTOP-5-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>DESKTOP-5-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

	<h1>UDP Patrol Status</h1>
	
	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-1-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-1-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-2-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-2-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-3-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-3-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-4-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-4-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>


<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp1.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-5-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-5-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	
	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp2.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-6-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-6-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>		
	
<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp3.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-7-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-7-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp4.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-8-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-8-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp5.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-9-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-9-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<?PHP 
$ts_ip = "1www.viber.com.edgekey.net.udp6.sikat.ml"; // Change to your server's IP external or domain name 
$ts_port = "80"; // Make sure this port is open on the router or firewall 

$output = @fsockopen("$ts_ip", $ts_port, $errno, $errstr, 2); 
socket_set_timeout($output, 000002); 

if (!$output) { 
    echo "<FONT COLOR=#DD0000><B>UDP=PATROL-10-OFFLINE</B></FONT>"; 
} else { 
    echo "<FONT COLOR=#00DD00><B>UDP=PATROL-10-ONLINE</B></FONT>"; 
} 
@fclose($output); 
?>

<br>	

<br>


</script>
</body>

</html>